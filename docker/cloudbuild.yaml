steps:
  - name: gcr.io/cloud-builders/docker
    args:
      - build
      - --file=docker/Dockerfile
      - --tag=${_IMAGE}
      - .
images:
  - ${_IMAGE}
timeout: 7200s
options:
  machineType: E2_HIGHCPU_8
